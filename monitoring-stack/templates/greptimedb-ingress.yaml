{{- if .Values.greptimeIngress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "monitoring-stack.fullname" . }}-greptimedb
  labels:
    {{- include "monitoring-stack.labels" . | nindent 4 }}
    app.kubernetes.io/component: greptimedb
  annotations:
    alb.ingress.kubernetes.io/scheme: {{ .Values.greptimeIngress.scheme | default "internet-facing" }}
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/backend-protocol: HTTP
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS":443}]'
    alb.ingress.kubernetes.io/ssl-policy: ELBSecurityPolicy-TLS13-1-2-2021-06
    alb.ingress.kubernetes.io/healthcheck-protocol: HTTP
    alb.ingress.kubernetes.io/healthcheck-path: /health
    alb.ingress.kubernetes.io/success-codes: "200"
    {{- if .Values.greptimeIngress.certificateArn }}
    alb.ingress.kubernetes.io/certificate-arn: {{ .Values.greptimeIngress.certificateArn }}
    {{- end }}
    {{- if .Values.greptimeIngress.groupName }}
    alb.ingress.kubernetes.io/group.name: {{ .Values.greptimeIngress.groupName }}
    {{- end }}
    {{- with .Values.greptimeIngress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: alb
  rules:
    - host: {{ .Values.greptimeIngress.host | required "greptimeIngress.host is required" }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-greptimedb-standalone
                port:
                  number: 4000
{{- end }}
